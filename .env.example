# Optional for the public marketing site. Required only when the integration is used.
# Use a direct/session-pooler PostgreSQL URL; configure verified TLS for Supabase.
DATABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only; bypasses RLS. Never prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
